---
coolify_install_enabled: "{{ install_coolify | default(false) }}"
coolify_installer_url: https://cdn.coollabs.io/coolify/install.sh
# Obrigatório quando habilitado: SHA-256 do script revisado pelo operador.
coolify_installer_sha256: ""
coolify_install_marker: /data/coolify/source/.env
