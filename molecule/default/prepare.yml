---
- name: Preparar container Debian
  hosts: all
  gather_facts: false
  tasks:
    - name: Instalar Python para o Ansible
      ansible.builtin.raw: >-
        apt-get update &&
        DEBIAN_FRONTEND=noninteractive apt-get install -y python3 python3-apt ca-certificates
      changed_when: true
