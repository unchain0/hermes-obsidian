---
- name: Preparar VPS para Hermes
  hosts: all
  become: true
  gather_facts: true
  roles:
    - role: common
    - role: docker
      when: install_docker | bool
    - role: coolify
      when: install_coolify | bool
